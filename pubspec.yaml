name: rexios_dev
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^1.10.5
  firebase_analytics: ^9.0.2
  flutter_markdown: ^0.6.2
  url_launcher: ^6.0.7
  get: ^4.2.4
  pub_api_client:
    git:
      url: https://github.com/Rexios80/pub_api_client
      ref: 6bc80bc08bc601f32840c0dbdc52306aaece46d4
  github: ^9.0.0
  font_awesome_flutter: ^10.0.0-beta

dev_dependencies:
  flutter_test:
    sdk: flutter

  rexios_lints: ^1.0.0
  flutter_native_splash: ^1.2.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/strings/

flutter_native_splash:
  color: '#042a49'
  image: 'assets/images/icon.png'
  android: false
  ios: false